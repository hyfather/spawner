<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Get Reconcile App</title>
    <meta name="viewport" content="width=device-width,
                                   initial-scale=1.0">
    <meta name="description" content="Seize Control of your Shared
                                      Expenses.">
    <meta name="author" content="hyfather">

    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>

    <link href="http://fonts.googleapis.com/css?family=Pacifico"
          rel="stylesheet" type="text/css">
    <link href="http://fonts.googleapis.com/css?family=Arimo"
          rel="stylesheet" type="text/css">
    <link href="http://fonts.googleapis.com/css?family=Trocchi"
          rel="stylesheet" type="text/css">

  </head>

  <body>
    <div class="container-narrow">
      <div class="masthead">
        <ul class="nav nav-pills pull-right">
          <li><a href="/">Home</a></li>
          <li><a href="/get_it">Get Reconcile App</a></li>

          <% if current_user %>
          <li><%= link_to "Account", reconcile_apps_path %></li>
          <li><%= link_to "Logout", destroy_user_session_path, :method => :delete %></li>
          <% else %>
          <li><%= link_to "Login", new_user_session_path %></li>
          <% end %>

          <li><a href="<%= about_path %>">About</a></li>
        </ul>
        <h2 class="logo" >Reconcile App</h2>
      </div>

      <hr>

      <% p flash %>

      <% if flash[:alert] -%>
      <p class='alert'><%=h flash[:alert] %></p> <hr>
      <% end -%>
      <% if flash[:notice] -%>
      <p class='notice'><%=h flash[:notice] %></p> <hr>
      <% end -%>


      <%= yield %>
    </div> <!-- /container -->
  </body>
</html>
